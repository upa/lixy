type: txt
priority: 840
help: Interface that is used for EID
allowed: /opt/vyatta/sbin/vyatta-interface.pl --show=ethernet

syntax:expression: exec \
	"if [ ! -d /sys/class/net/$VAR(@) ]; then \
		echo \"interface $VAR(@) does not exist\"
		exit 1; \
	fi"

update: sudo /usr/local/sbin/lixyctl eid $VAR(../@) interface $VAR(@)
delete: sudo /usr/local/sbin/lixyctl eid $VAR(../@) interface delete
